# DataForge 造数工厂 — 后端依赖（固定版本）
# 对应架构文档 2.1 技术选型总表

# Web 框架
fastapi==0.111.0
uvicorn[standard]==0.30.3
python-multipart==0.0.9

# ORM 与数据库驱动
SQLAlchemy[asyncio]==2.0.31
aiomysql==0.2.0
PyMySQL==1.1.1

# 数据校验与配置管理
# 说明：为兼容 Python 3.13/3.14，pydantic 放宽到 >=2.10.4（pydantic-core 需提供新解释器 wheel）
pydantic>=2.10.4
pydantic-settings>=2.3.4

# 异步任务与缓存
celery>=5.5.0
# Redis 服务端为 5.x（不支持 RESP3 的 HELLO 握手），客户端必须 <8.x
redis==5.0.7

# 认证与安全
python-jose[cryptography]>=3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 读取 bcrypt.__about__（4.1 已移除）会在校验时报 AttributeError 但功能可用（warning）；
# bcrypt 需 >=4.1 才有 Python 3.13/3.14 的预编译 wheel，避免本地源码编译
bcrypt>=4.2.0
cryptography>=43.0.0

# HTTP 客户端（心跳检测等）
httpx==0.27.0

# 结构化日志
structlog==24.2.0

# 数据库迁移
alembic==1.13.2

# Nacos 配置中心 / 服务注册（官方 SDK）
nacos-sdk-python==2.0.0
PyYAML==6.0.1
